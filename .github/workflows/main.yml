name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main
  #     - Dev

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      # 1. Obter o Código
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # 2. Configurar o Node.js e NPM
      - name: 🛠️ Setup Node.js and NPM
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3. Instala o NPM
      - name: 📦 Install NPM and Build
        shell: bash
        working-directory: ./my-portifolio
        run: |
          npm install 
          npm run build

      # 5. Fazer o Deploy do Conteúdo da pasta
      - name: 📂 Sync files (FTP Deploy)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_SENHA }}
          local-dir: ./my-portifolio/dist/
          server-dir: /public_html/joaoroblez/
          exclude: |
            public/**
            plugin/**
            Banco_de_Dados_CR/**
            crons/**
            storage/**
            .github
            .github/**
            .env
            composer.json
            composer.lock
            .htaccess
            config.php
            config.js

      # 5️⃣ Setup PHP
      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer

      # 6️⃣ Install Composer dependencies (PHPMailer)
      - name: 📦 Install Composer dependencies
        working-directory: ./Portifolio-Api
        run: |
          rm -rf vendor 
          composer install --no-dev --optimize-autoloader

      # 5️⃣ Deploy do BACKEND
      - name: 🐘 Deploy Backend (PHP API)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_SENHA }}
          local-dir: ./Portifolio-Api/
          server-dir: /public_html/joaoroblez/api/
          exclude: |
            **/.git*
            **/.github/**
            **/.env
            **/node_modules/**
            **/storage/**
